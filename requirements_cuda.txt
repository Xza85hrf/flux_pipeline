# Extra index for CUDA wheels. cu128 hosts the widest set of Python 3.13
# wheels (torch/torchvision/torchaudio up to 2.11 / 0.26). The CUDA 12.8
# runtime is backward compatible with any NVIDIA driver >= R550 (CUDA 12.x+).
--extra-index-url https://download.pytorch.org/whl/cu128

# Core dependencies with CUDA support.
# Pin with the +cu128 local-version suffix so pip selects from the
# PyTorch index rather than the CPU-only generic wheel on PyPI.
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128
# xformers wheels on PyPI link against the torch ABI and follow torch's
# CUDA runtime; 0.0.29 is the first release compatible with torch 2.6+.
xformers>=0.0.29
cuda-python>=12.8.0

# Machine learning libraries
diffusers>=0.36.0
transformers>=4.57.0
accelerate>=1.12.0
safetensors>=0.4.5
huggingface-hub>=0.27.0
sentencepiece>=0.1.99
protobuf
grpcio
grpcio-tools

# Utility libraries (same as requirements.txt)
numpy>=2.1
Pillow>=11.0
psutil>=6.1
tqdm>=4.66
pynvml>=11.5
fsspec>=2024.10
PyYAML>=6.0
networkx>=3.4
sympy>=1.13
filelock>=3.16
typing_extensions>=4.12
regex>=2024.9
nltk>=3.9
jinja2>=3.1

# Web and API libraries (same as requirements.txt)
gradio>=5.4
fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.12
aiofiles>=23.2
orjson>=3.10
websockets>=16.0

# Image processing libraries (same as requirements.txt)
opencv-python>=4.10
scikit-image>=0.24

# Miscellaneous libraries (same as requirements.txt)
cachetools>=5.3
rich>=13.9

# Development tools (same as requirements.txt)
ruff>=0.7
black>=24.10

# Testing tools (same as requirements.txt)
pytest>=8.3
pytest-cov>=6.0
mypy>=1.14
