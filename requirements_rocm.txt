# Extra index for ROCm wheels (Linux only; AMD does not publish ROCm
# wheels for Windows). rocm6.3 hosts the latest stable torch 2.9.1
# family with Python 3.13 wheels.
--extra-index-url https://download.pytorch.org/whl/rocm6.3

# Core dependencies with ROCm support.
# Note: the package name is 'torch' (with a +rocmX.Y local-version
# suffix), not 'torch-hip' — the previous pin referenced a distribution
# that does not exist on PyPI or the PyTorch index.
torch==2.9.1+rocm6.3
torchvision==0.24.1+rocm6.3
torchaudio==2.9.1+rocm6.3

# Machine learning libraries
diffusers>=0.36.0
transformers>=4.57.0
accelerate>=1.12.0
safetensors>=0.4.5
huggingface-hub>=0.27.0
sentencepiece>=0.1.99
protobuf
grpcio
grpcio-tools

# Utility libraries (same as requirements.txt)
numpy>=2.1
Pillow>=11.0
psutil>=6.1
tqdm>=4.66
fsspec>=2024.10
PyYAML>=6.0
networkx>=3.4
sympy>=1.13
filelock>=3.16
typing_extensions>=4.12
regex>=2024.9
nltk>=3.9
jinja2>=3.1

# Web and API libraries (same as requirements.txt)
gradio>=5.4
fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.12
aiofiles>=23.2
orjson>=3.10
websockets>=12.0

# Image processing libraries (same as requirements.txt)
opencv-python>=4.10
scikit-image>=0.24

# Miscellaneous libraries (same as requirements.txt)
cachetools>=5.3
rich>=13.9

# Development tools (same as requirements.txt)
ruff>=0.7
black>=24.10

# Testing tools (same as requirements.txt)
pytest>=8.3
pytest-cov>=6.0
mypy>=1.14
